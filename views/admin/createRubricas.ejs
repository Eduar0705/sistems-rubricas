<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Crear Rúbrica' %></title>
    <link rel="stylesheet" href="../css/home.css">
    <link rel="stylesheet" href="../css/crearRubrica.css">
    <link rel="shortcut icon" href="../img/favicon.ico" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body>
    <%- include('../inc/menu.ejs') %>

    <main class="main-content">
        <header class="header">
            <div class="header-left">
                <button class="mobile-menu-toggle" id="mobileMenuToggle">
                    <i class="fas fa-bars"></i>
                </button>
                <h1 class="page-title"><%= title || 'Crear Rúbrica' %></h1>
            </div>
            <div class="header-right">
                <button class="header-btn" onclick="window.location.href = '/admin/config'">
                    <i class="fas fa-cog"></i>
                </button>
            </div>
        </header>

        <div style="padding: 30px;" id="crear">
            <div class="form-container">
                <div class="card">
                    <div class="card-header">
                        <h2 class="card-title">
                            <i class="fas fa-plus-circle"></i>
                            Crear Nueva Rúbrica
                        </h2>
                    </div>
                    <div class="card-content">
                        <div class="alert alert-info">
                            <i class="fas fa-info-circle"></i>
                            Complete todos los campos requeridos. Puede agregar múltiples criterios y niveles de desempeño.
                        </div>

                        <form id="rubricaForm" method="post" action="/envioRubrica">
                            <!-- Información General -->
                            <div class="form-group">
                                <label for="nombreRubrica">Nombre de la Rúbrica *</label>
                                <input type="text" id="nombreRubrica" class="form-input" required
                                    placeholder="Ej: EVALUACIÓN INDIVIDUAL DISTRIBUCIÓN DE FRECUENCIAS">
                            </div>

                            <div class="form-row">
                                <div class="form-group">
                                    <label for="materia">Materia *</label>
                                    <select id="materia" class="form-select" required>
                                        <option value="">Seleccionar materia</option>
                                        <% if(materias && materias.length > 0) { %>
                                            <% materias.forEach(function(materia) { %>
                                                <option value="<%= materia.codigo %>"><%= materia.nombre %></option>
                                            <% }); %>
                                        <% } %>
                                    </select>
                                </div>
                                <div class="form-group">
                                    <label for="seccion">Sección *</label>
                                    <select id="seccion" class="form-select" required>
                                        <option value="">Seleccionar sección</option>
                                        <% if(secciones && secciones.length > 0) { %>
                                            <% secciones.forEach(function(seccion) { %>
                                                <option value="<%= seccion.id %>"><%= seccion.codigo %> - <%= seccion.materia_codigo %></option>
                                            <% }); %>
                                        <% } %>
                                    </select>
                                </div>
                            </div>

                            <div class="form-row">
                                <div class="form-group">
                                    <label for="fechaEvaluacion">Fecha de Evaluación *</label>
                                    <input type="date" id="fechaEvaluacion" class="form-input" required>
                                </div>
                                <div class="form-group">
                                    <label for="porcentaje">Porcentaje de Evaluación (%) *</label>
                                    <input type="number" id="porcentaje" class="form-input" required
                                        min="0" max="100" step="0.01" placeholder="10">
                                </div>
                                <div class="form-group">
                                    <label for="tipoEvaluacion">Tipo de Evaluación *</label>
                                    <select id="tipoEvaluacion" class="form-select" required>
                                        <option value="">Seleccionar tipo</option>
                                        <option value="individual">Individual</option>
                                        <option value="grupal">Grupal</option>
                                        <option value="oral">Oral</option>
                                        <option value="taller">Taller</option>
                                        <option value="presentacion">Presentación</option>
                                        <option value="proyecto">Proyecto</option>
                                    </select>
                                </div>
                            </div>

                            <div class="form-group">
                                <label for="competencias">Competencias</label>
                                <textarea id="competencias" class="form-textarea" rows="3"
                                    placeholder="Describa las competencias que se evaluarán..."></textarea>
                            </div>

                            <div class="form-group">
                                <label for="instrucciones">Instrucciones</label>
                                <textarea id="instrucciones" class="form-textarea" rows="3"
                                    placeholder="Instrucciones para la evaluación..."></textarea>
                            </div>

                            <!-- Criterios de Evaluación -->
                            <div class="criterios-section">
                                <div class="section-header">
                                    <h3><i class="fas fa-list-check"></i> Criterios de Evaluación</h3>
                                    <button type="button" class="btn-secondary" onclick="agregarCriterio()">
                                        <i class="fas fa-plus"></i>
                                        Agregar Criterio
                                    </button>
                                </div>

                                <div id="criteriosList">
                                    <!-- Los criterios se agregarán dinámicamente aquí -->
                                </div>
                            </div>

                            <!-- Botones de Acción -->
                            <div class="form-actions">
                                <button type="button" class="btn-secondary" onclick="window.location.href='/admin/rubricas'">
                                    <i class="fas fa-times"></i>
                                    Cancelar
                                </button>
                                <button type="submit" class="btn-primary">
                                    <i class="fas fa-save"></i>
                                    Crear Rúbrica
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        <% if (typeof locals.mensaje != 'undefined' || typeof mensaje != 'undefined') { %>
            <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
            <script>
                Swal.fire({
                    icon: '<%= (locals.mensaje || mensaje).includes("Error") ? "error" : "success" %>',
                    title: 'Información',
                    text: '<%= locals.mensaje || mensaje %>',
                    confirmButtonText: 'Aceptar'
                });
            </script>
        <% } %>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script src="../js/crear-rubrica.js"></script>
</body>
</html>